set -g mouse on # Enables mouse features (e.g scrolling, text selection + copy/paste, etc.)

bind -n S-Left swap-window -t -1\; select-window -t -1
bind -n S-Right swap-window -t +1\; select-window -t +1
